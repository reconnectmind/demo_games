1. Есть баг в присваивании символов клавиатуры кнопкам, на размере 5х7 где-то в числах по порядку появляются кнопки без ключа с клавиатуры, их возможно нажать только мышью 
2. По времени механике запуска игры. У нас есть политика роста сложности, видимо надо сделать отдельно также политику завершения игры, но она уже является не частью gameinstance, а частью той системы, которая его запускает. Возможно это декоратор, или game series laucher -- надо обсудить архитектуру вокруг инстанса игры. В экспериментах могут быть зоны с фиксированным заданным временем под игры. Это может быть одна длинная игра с постоянной адаптивной сложностью, типа сквош. Может быть ситуация, когда игра просто перезапускается до тех пор, пока не закончится время. Соответственно, у нас несколько стратегий исполнения игр будет. 1) По времени, если игра закончилась, а время нет, запускается автоматом следующая. Если время закончилось, а игра нет -- она останавливается, 2) По количеству игр, 3) Первое что достигнет (количество / время). 
3. Давай проектировать приложение для эксперимента. На вход приложение принимает протокол. Это json конфиг параметров эксперимента, определяющий сценарий того, что происходит внутри. Какие игры, какие параметры сложности, какие стратегии запуска. Отталкиваемся от game commments (edited).docx. Давай рассмотрим бейзлайн как отдельную игру -- это просто текст рассказывающий участнику что надо сидеть спокойно, с таймером на экране (пусть это будет параметр игры, неизвестно нужен ли он). С тз UX у нас должна получиться последовательность: 1) ввод данных участника, настройка эксперимента, параметров (в тч длительности секций бейзлайн -> обучение -> игра), запуск, 2) бейзлайн, 3) обучение, 4) игра. Спроектировать можно универсально, чтобы в последующем эти блоки можно было добавлять новые.

---

## Статус

**1. Клавиши на ячейках — сделано.** Набор клавиш indexed-действия расширен до 36 (цифровой ряд плюс буквы рядами клавиатуры) и стал свойством действия в манифесте: у числового поля он `letters-first`, потому что сами ячейки подписаны цифрами. Ячеек без клавиши больше нет ни на одном уровне; проверяется `tests/hints.test.ts` на верхнем уровне каждой игры, пропуск дальних индексов из теста убран.

**2. Политика завершения — сделано, живёт в `packages/protocol`.** `TerminationPolicy` устроена как `DifficultyPolicy`: `byTime`, `byRuns`, `runNoLongerThan`, `firstOf`. `SectionRunner` владеет перезапусками, политиками сложности задач (уровень переживает перезапуск и переход между участками), паузой, снимком и восстановлением участка. Когда время вышло посреди блока, раннер шлёт игре `protocol finish` — ядро закрывает блок само и отдаёт частичную сводку, а не обрывается. Записи журнала получили `sectionId` и `runIndex`.

**3. Приложение эксперимента — фундамент готов, Tauri-хост ещё нет.** Протокол стал документом со схемой `packages/protocol/schema/protocol.schema.json`; пилот на 110 минут лежит в `examples/reconnect-pilot.json`. Baseline реализован как обычная микроигра `org.reconnect.baseline` с параметрами `durationMs`, `showTimer`, `text`. Порядок `AB`/`BA` назначается детерминированно по `participantId`. Витрина умеет проигрывать сценарий целиком со сжатыми участками.

Осталось по третьему пункту: экран оператора с вводом данных участника до старта, `apps/experiment` на Tauri, LSL-маркеры и экспорт в файл вместо памяти.