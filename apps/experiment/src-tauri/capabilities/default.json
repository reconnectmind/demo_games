{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal permissions for the experiment renderer",
  "windows": ["main"],
  "permissions": ["core:default"]
}
